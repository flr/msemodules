% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa.R
\name{shortcut_devs}
\alias{shortcut_devs}
\title{Generate deviation series for shortcut assessments}
\usage{
shortcut_devs(
  om,
  Fcv = 0.212,
  Fphi = 0.423,
  SSBcv = 0,
  SSBphi = 0,
  bias.correct = FALSE
)
}
\arguments{
\item{om}{An operating model object used only to define dimensions for the
generated deviation series. The function uses \code{dims(om)$iter} for the
number of iterations and \code{dimnames(om)$year} for the year dimension.}

\item{Fcv}{Numeric. Standard deviation on the log scale for the fishing
mortality deviation process. Defaults to \code{0.212}.}

\item{Fphi}{Numeric. Autocorrelation parameter for the fishing mortality
deviation process. Defaults to \code{0.423}.}

\item{SSBcv}{Numeric. Standard deviation on the log scale for the spawning
stock biomass deviation process. Defaults to \code{0}.}

\item{SSBphi}{Numeric. Autocorrelation parameter for the spawning stock
biomass deviation process. Currently not used in the implementation, which
fixes the \code{SSB} autocorrelation at \code{0}. Defaults to \code{0}.}

\item{bias.correct}{Logical. If \code{TRUE}, applies bias correction when
generating the lognormal AR(1) deviates. Passed to \code{\link[FLCore:rlnormar1]{FLCore::rlnormar1()}}.
Defaults to \code{FALSE}.}
}
\value{
An \code{FLQuants} object with two elements:
\itemize{
\item \code{F}: a lognormal AR(1) deviation series for fishing mortality;
\item \code{SSB}: a lognormal deviation series for spawning stock biomass.
}
}
\description{
Creates stochastic multiplicative deviation series for use in shortcut stock
assessments, typically representing uncertainty or observation error applied
to fishing mortality and spawning stock biomass metrics.
}
\details{
The deviations are generated as lognormal autoregressive series and returned
as an \code{FLQuants} object with components for \code{F} and \code{SSB}.

The function generates deviations using \code{\link[FLCore:rlnormar1]{FLCore::rlnormar1()}}, with:
\itemize{
\item \code{n = dims(om)$iter} iterations;
\item \code{years = dimnames(om)$year};
\item \code{meanlog = 0}, so the process is centered on multiplicative value 1
on the log scale before optional bias correction;
\item separate variance and autocorrelation settings for \code{F} and \code{SSB}.
}

The returned object is suitable for direct use in \code{\link[=shortcut.sa]{shortcut.sa()}}, where the
deviations can be applied to derived stock metrics.
}
\section{Current implementation note}{

Although \code{SSBphi} is provided as an argument, the function currently hardcodes
\code{rho=0} for the \code{SSB} process. This means the SSB deviations are independent
through time regardless of the value supplied to \code{SSBphi}.
}

\examples{
\dontrun{
# Generate default shortcut deviations
devs <- shortcut_devs(om)

# More variable and autocorrelated F deviations
devs <- shortcut_devs(om, Fcv = 0.3, Fphi = 0.6)

# With bias correction
devs <- shortcut_devs(om, bias.correct = TRUE)
}

}
\seealso{
\code{\link[=shortcut.sa]{shortcut.sa()}}, \code{\link[FLCore:rlnormar1]{FLCore::rlnormar1()}}, \code{\link[FLCore:FLQuants]{FLCore::FLQuants()}}
}
\author{
Iago Mosqueira, WMR; Ernesto Jardim, IPMA
}
\keyword{models}
