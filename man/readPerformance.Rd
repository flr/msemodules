% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{readPerformance}
\alias{readPerformance}
\title{Read a performance table from file}
\usage{
readPerformance(file = "model/performance.dat.gz")
}
\arguments{
\item{file}{A character string giving the path to the performance table file.
Defaults to \code{"model/performance.dat.gz"}.}
}
\value{
A \code{data.table} containing performance statistics. The returned table:
\itemize{
\item has columns coerced to the expected classes;
\item is keyed by \code{om}, \code{type}, \code{run}, \code{biol}, \code{mp}, \code{statistic}, and \code{year};
\item has columns ordered as
\code{om}, \code{type}, \code{run}, \code{mp}, \code{biol}, \code{statistic}, \code{name}, \code{desc}, \code{year},
\code{iter}, \code{data};
\item converts grouping columns such as \code{om}, \code{type}, \code{run}, \code{mp}, \code{biol},
\code{statistic}, and \code{label} to factors when present in the file.
}
}
\description{
Reads a performance statistics table previously written by
\code{\link[=writePerformance]{writePerformance()}}, restores column classes, keying, and column order, and
returns the result as a \code{data.table}.
}
\details{
This function is the standard reader for performance tables stored on disk,
typically in compressed text format such as \code{model/performance.dat.gz}.

The function uses \code{\link[data.table:fread]{data.table::fread()}} to import the table and explicitly
sets classes for commonly used columns in performance output:
\itemize{
\item \code{type}, \code{run}, \code{mp}, and \code{biol} are read as character;
\item \code{year} and \code{data} are read as numeric;
\item \code{iter} is read as character.
}

After reading, a key is assigned with \code{\link[data.table:setkey]{data.table::setkey()}} to facilitate
fast joins and subsetting. The function also standardizes column order so
downstream utilities can assume a stable structure.

If a \code{label} column is present in the file, it is converted to a factor along
with the other categorical columns.
}
\examples{
\dontrun{
# Read the default performance table
dat <- readPerformance()

# Read from a specific file
dat <- readPerformance("results/performance.dat.gz")

# Inspect the structure
str(dat)
key(dat)
}

}
\seealso{
\code{\link[mse:performance]{mse::performance()}} \code{\link[=writePerformance]{writePerformance()}}, \code{\link[=summaryPerformance]{summaryPerformance()}}, \code{\link[=labelPerformance]{labelPerformance()}}
}
\author{
Iago Mosqueira, WMR
}
\keyword{file}
