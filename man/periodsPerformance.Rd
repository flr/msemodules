% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{periodsPerformance}
\alias{periodsPerformance}
\title{Aggregate performance statistics by period}
\usage{
periodsPerformance(x, periods)
}
\arguments{
\item{x}{A \code{data.table} containing performance statistics. It must include at
least the columns \code{year}, \code{data}, \code{type}, \code{mp}, \code{statistic}, \code{name},
\code{desc}, and \code{iter}. If a \code{label} column is present, it is retained in the
grouping structure.}

\item{periods}{A vector or list defining the periods over which means should
be computed. Each element should contain the years belonging to one period.
Named elements are used as period labels; unnamed elements are labelled
automatically from the year range.}
}
\value{
A \code{data.table} with one row per grouping combination and period, containing:
\itemize{
\item the grouping variables inherited from \code{x};
\item \code{period}, the name of the period;
\item \code{year}, a compact character representation of the year span;
\item \code{data}, the mean of \code{data} over the years in that period.
}
}
\description{
Computes mean performance values over user-defined periods and returns a
summarized performance table.
}
\details{
This function is useful for collapsing annual performance time series into
broader periods, such as short-, medium-, and long-term intervals, while
preserving the main grouping structure of the original performance table.

The \code{periods} argument is coerced to a list. For each period:
\itemize{
\item if the element has length greater than one, the display label in the
output \code{year} column is built as \code{"start-end"}, where \code{end} uses the last
two digits of the final year;
\item if the element has length one, that year is used directly.
}

If period names are missing, they are replaced by the derived year labels.

Means are computed with \code{na.rm=TRUE}, so missing values in \code{data} are ignored.

When \code{x} contains a \code{label} column, aggregation is done by:
\code{type}, \code{mp}, \code{label}, \code{statistic}, \code{name}, \code{desc}, and \code{iter}.

Otherwise aggregation is done by:
\code{type}, \code{mp}, \code{statistic}, \code{name}, \code{desc}, and \code{iter}.
}
\section{Output structure}{

The resulting table is intended to remain compatible with downstream plotting
or reporting workflows based on summarized performance indicators.
}

\examples{
\dontrun{
dat <- readPerformance()

# Define named periods
per <- list(
  short = 2021:2025,
  medium = 2026:2035,
  long = 2036:2050
)

res <- periodsPerformance(dat, per)

# Unnamed periods will be labelled automatically
res2 <- periodsPerformance(dat, list(2021:2025, 2026:2030))
}

}
\seealso{
\code{\link[=labelPerformance]{labelPerformance()}}, \code{\link[=summaryPerformance]{summaryPerformance()}}
}
\author{
Iago Mosqueira, WMR
}
\keyword{manip}
