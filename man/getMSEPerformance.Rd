% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{getMSEPerformance}
\alias{getMSEPerformance}
\title{Build a performance table from MSE result files}
\usage{
getMSEPerformance(path, pattern = "*.rds")
}
\arguments{
\item{path}{A character string giving the directory containing serialized MSE
result files.}

\item{pattern}{A character string giving the file pattern used by
\code{\link[base:list.files]{base::list.files()}} to identify files to read. Defaults to \code{"*.rds"}.}
}
\value{
A \code{data.table} created by row-binding performance results from all matching
files.
}
\description{
Reads a set of serialized MSE result files from disk and combines their
performance statistics into a single \code{data.table}.
}
\details{
For each file found in \code{path} matching \code{pattern}, the object is read using
\code{\link[=readRDS]{readRDS()}}. Then:
\itemize{
\item if the object already inherits from \code{data.table}, it is returned as-is;
\item otherwise, \code{\link[=performance]{performance()}} is called on the object to derive the
performance table.
}

All resulting tables are combined with \code{\link[data.table:rbindlist]{data.table::rbindlist()}} using
\code{fill=TRUE}, so files with slightly different column structures can still be
merged.

This function is intended as a convenient batch importer for previously saved
MSE outputs or already extracted performance tables.
}
\section{Typical use cases}{

\itemize{
\item combining performance output from multiple management procedures;
\item importing a mixture of saved raw MSE objects and precomputed
\code{data.table} performance summaries;
\item preparing inputs for \code{\link[=writePerformance]{writePerformance()}} or downstream summaries.
}
}

\examples{
\dontrun{
# Read and combine performance tables from a folder of MSE results
dat <- getMSEPerformance("mse")

# Use a custom file pattern
dat <- getMSEPerformance("mse", pattern = "scenario_.*\\\\.rds$")
}

}
\seealso{
\code{\link[=getOMPerformance]{getOMPerformance()}}, \code{\link[=readPerformance]{readPerformance()}}, \code{\link[=writePerformance]{writePerformance()}}
}
\author{
Iago Mosqueira, WMR
}
\keyword{file}
