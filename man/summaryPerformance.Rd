% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{summaryPerformance}
\alias{summaryPerformance}
\title{Summarize a performance table}
\usage{
summaryPerformance(file = "model/performance.dat.gz")
}
\arguments{
\item{file}{Either:
\itemize{
\item a character string giving the path to a performance table file, or
\item a \code{data.table} already containing performance statistics.
}
Defaults to \code{"model/performance.dat.gz"}.}
}
\value{
Invisibly returns \code{TRUE}. The main effect of the function is to print a brief
summary to the console.
}
\description{
Produces a compact summary of a performance statistics table, either from a
file on disk or from an already loaded \code{data.table}.
}
\details{
The function reports the number of operating models (\code{om}), management
procedure types (\code{type}), and management procedures (\code{mp}), and computes a
grouped summary by \code{om}, \code{type}, and \code{run}.

If \code{file} is not a \code{data.table}, it is first read with \code{\link[=readPerformance]{readPerformance()}}.

The grouped summary computes, for each combination of \code{om}, \code{type}, and
\code{run}:
\itemize{
\item \code{years}: the minimum and maximum years pasted as a range;
\item \code{frq}: the spacing between the first two unique years, intended as an
indication of temporal frequency;
\item \code{iter}: the number of unique iterations.
}

A second summary counts the number of unique values in \code{om}, \code{type}, and
\code{mp}, and prints them as a one-line overview.

The per-group summary object is created internally but not currently printed,
except for the one-line counts written with \code{\link[base:cat]{base::cat()}}. The commented code
in the function suggests this output may later be extended to print a tree or
tabular summary.
}
\section{Current behavior and caveats}{

\itemize{
\item The function is primarily intended for quick console inspection.
\item The \code{frq} calculation assumes at least two unique years are present
within each group; otherwise it may return \code{numeric(0)} or \code{NA}.
\item The grouped summary is not currently returned, only computed
internally.
}
}

\examples{
\dontrun{
# Summarize from default file
summaryPerformance()

# Summarize from a specific file
summaryPerformance("results/performance.dat.gz")

# Summarize an object already in memory
dat <- readPerformance("results/performance.dat.gz")
summaryPerformance(dat)
}

}
\seealso{
\code{\link[=readPerformance]{readPerformance()}}, \code{\link[=writePerformance]{writePerformance()}}
}
\author{
Iago Mosqueira, WMR
}
\keyword{utilities}
