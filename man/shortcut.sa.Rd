% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa.R
\name{shortcut.sa}
\alias{shortcut.sa}
\title{Shortcut stock assessment including error on a status metric}
\usage{
shortcut.sa(
  stk,
  idx,
  metric = "ssb",
  SSBdevs = met \%=\% 1,
  devs = SSBdevs,
  args,
  tracking,
  ...
)
}
\arguments{
\item{stk}{An \code{FLStock}-like object representing the stock available to the
shortcut assessment. The object is truncated to the terminal data year
\code{dy}.}

\item{idx}{An index object. Currently included for interface compatibility
with other assessment functions, but not used directly inside this
function.}

\item{metric}{A character string naming the function used to compute the
assessment metric from \code{stk}. Defaults to \code{"ssb"}. The named function is
called via \code{\link[base:do.call]{base::do.call()}}.}

\item{SSBdevs}{An object containing deviations to apply to the computed
metric. Defaults to \code{met \%=\% 1}, i.e. a unit deviation structure matching
the dimensions of the computed metric. Typically this is an \code{FLQuant} or
compatible object.}

\item{devs}{Deprecated alias or shorthand for \code{SSBdevs}. Defaults to
\code{SSBdevs}.}

\item{args}{A list-like object containing dimensions and assessment settings
needed by \code{\link[FLCore:spread]{FLCore::spread()}}. In particular, this function assumes that variables
such as \code{dy}, \code{y0}, \code{ay}, and \code{stock} become available after calling
\code{spread(args)}.}

\item{tracking}{A tracking object updated in-place through  to record
convergence of the shortcut assessment.}

\item{...}{Additional arguments passed to the metric function named in
\code{metric}.}
}
\value{
A named list with components:
\itemize{
\item \code{stk}: the stock object truncated to the terminal data year \code{dy};
\item \code{ind}: an \code{FLQuants} object containing the derived indicator series,
named according to \code{metric};
\item \code{tracking}: the updated tracking object with convergence information
recorded.
}
}
\description{
Performs a simplified or "shortcut" stock assessment by truncating an
operating model stock object to the current data year, computing a selected
stock metric, applying stochastic deviations, and returning the resulting
indicator together with the truncated stock and updated tracking object.
}
\details{
This function is intended for use inside management procedure workflows where
a full assessment model is not run, but an index or assessment quantity is
approximated from the operating model using a direct transformation of stock
state and observation-error-like deviations.

The function proceeds as follows:
\enumerate{
\item values in \code{args} are unpacked using \code{\link[FLCore:spread]{FLCore::spread()}};
\item the stock object is truncated to \code{end=dy} using \code{\link[=window]{window()}};
\item the requested metric is computed by calling the function named in
\code{metric} on \code{stk};
\item the result is collapsed across units using \code{\link[FLCore:dimSummaries]{FLCore::unitSums()}};
\item stochastic deviations are applied over the period from \code{y0} to \code{dy};
\item the resulting quantity is wrapped in an \code{\link[FLCore:FLQuants]{FLCore::FLQuants()}} object and named
with the selected metric;
\item convergence is recorded in \code{tracking} by setting
\code{"conv.est"} to \code{1} for year \code{ay} and biological unit \code{stock}.
}
}
\section{Assumptions}{

\itemize{
\item the function named in \code{metric} exists and accepts \code{stk} as its first
argument;
\item \code{devs} or \code{SSBdevs} are dimensionally compatible with the metric
computed from \code{stk};
}
}

\section{Notes}{

\itemize{
\item \code{idx} is currently unused but may be retained for consistency with
other assessment-method signatures.
\item \code{devs} is currently used to build the indicator, even though
\code{SSBdevs} is the more explicit argument name.
}
}

\examples{
\dontrun{
# dataset contains both OM (FLom) and OEM (FLoem)
data(plesim)
# Create shortcut deviances for F and SSB
devs <- shortcut_devs(om, Fcv = 0.3, Fphi = 0.6)
# Set control: sa and hcr
control <- mpCtrl(list(
  est = mseCtrl(method=shortcut.sa,
   args=list(devs=devs$SSB)),
  hcr = mseCtrl(method=hockeystick.hcr, args=list(lim=0,
  trigger=14000, target=0.18))))
# Runs mp
tes <- mp(om, ctrl=control, args=list(iy=2021, fy=2026))
}

}
\seealso{
\code{\link[=shortcut_devs]{shortcut_devs()}}, \code{\link[FLCore:ssb]{FLCore::ssb()}}
}
\author{
Iago Mosqueira, WMR; Ernesto Jardim, IPMA
}
\keyword{methods}
