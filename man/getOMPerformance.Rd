% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{getOMPerformance}
\alias{getOMPerformance}
\title{Build a performance table from operating model files}
\usage{
getOMPerformance(path, pattern = "*.rds", fy, ...)
}
\arguments{
\item{path}{A character string giving the directory containing serialized
operating model files.}

\item{pattern}{A character string giving the file pattern used by
\code{\link[base:list.files]{base::list.files()}} to identify files to read. Defaults to \code{"*.rds"}.}

\item{fy}{The final year to retain when windowing each operating model before
computing performance. Passed to \code{\link[=window]{window()}}.}

\item{...}{Additional arguments passed to \code{\link[=performance]{performance()}}.}
}
\value{
A \code{data.table} containing the row-bound performance statistics for all files
found in \code{path} matching \code{pattern}. An \code{om} column is added using the file
name with the \code{.rds} extension removed.
}
\description{
Reads a set of serialized operating model objects from disk, computes
performance statistics for each, and combines the results into a single
\code{data.table}.
}
\details{
For each file found:
\enumerate{
\item the object is read using \code{\link[=readRDS]{readRDS()}};
\item the \verb{$om} component is extracted;
\item the object is trimmed to \code{fy} using \code{\link[=window]{window()}};
\item \code{\link[=performance]{performance()}} is called on the resulting object;
\item an \code{om} column is added from the file basename.
}

The results are combined using \code{\link[data.table:rbindlist]{data.table::rbindlist()}}.

Warnings generated while computing performance are suppressed using
\code{\link[base:warning]{base::suppressWarnings()}}, which can be useful in batch processing but may
hide potentially important diagnostics.
}
\section{Assumptions}{

\itemize{
\item Each \code{.rds} file contains an object with an \verb{$om} component.
\item That component is compatible with \code{\link[=window]{window()}} and \code{\link[=performance]{performance()}}.
\item File names uniquely identify the operating model and can be used as
the \code{om} label.
}
}

\examples{
\dontrun{
# Build a performance table from OM files in a directory
dat <- getOMPerformance("om", fy = 2025)

# Pass additional arguments on to performance()
dat <- getOMPerformance("om", fy = 2025, probs = c(0.1, 0.5, 0.9))
}

}
\seealso{
\code{\link[=getMSEPerformance]{getMSEPerformance()}}, \code{\link[=writePerformance]{writePerformance()}}, \code{\link[=performance]{performance()}}
}
\author{
Iago Mosqueira, WMR
}
\keyword{file}
